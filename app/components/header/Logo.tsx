import React, { useEffect, useRef } from "react";
import gsap from "gsap";

const Logo: React.FC = () => {

  return (
    <div className="">
      {/* Main Logo */}
      <div
        className=""
      >
        NUIT BLANCHE
      </div>
    </div>
  );
};

export default Logo;
